@model StrangerRecord.Models.Entity.Identification
@using StrangerRecord.Service;

@{
    ViewBag.Title = "Details";
}
<div class="row mt-3">
    <div class="col-4">
        @Html.Partial("_DetailIdentificationPartial", Model)
    </div>
    <div class="col-8">
        @{ var lastcarte = DataProvider.GetLastCarte(Model.id); }
        @Html.Partial("_DetailCartePartial", lastcarte)
       
        @Html.Partial("_DetailSejourPartial", lastcarte)
         
    </div>
</div>


